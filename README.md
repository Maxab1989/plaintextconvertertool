# plaintextconvertertool
This app converts any messy text to plain text. The tool section has two container, the input and output. Users input the text and the tool automatically converts it to plain text in the output section. Files can be deployed to a node.js server
